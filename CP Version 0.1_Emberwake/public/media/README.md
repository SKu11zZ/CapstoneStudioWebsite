# Media Assets

Place final trailer stills, screenshots, key art, and cover art here.

The current site uses CSS-built cinematic placeholders so the production bundle does not rely on remote images or third-party CDNs. When replacing placeholders, update `src/data/content.ts` with local `/media/...` paths and keep the CSP in `public/_headers` aligned with the final asset strategy.
